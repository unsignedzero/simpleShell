date
ls -al | grep term
touch output
cat terminal.h | grep "/?*"
cat terminal.h | grep '^//' | uniq > output
cat terminal.h | grep '^//' | uniq | wc -l
cat output
wc < output
cat terminal.c | grep int | wc -l
rm output
exit

